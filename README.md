# conversion-wf-bash
A dummy bash script as example of simple workflow for a LabID workshop

`convert.sh` is a thin wrapper around `eubi <input_dir> <output_dir>`.
Install `requirements.txt` in your active Python environment so the `eubi` command is available on `PATH` before running the script.
