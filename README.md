# conversion-wf-bash
A dummy bash script as example of simple workflow for a LabID workshop

`convert.sh` is a thin wrapper around `eubi <input_dir> <output_dir>`.

# Install
`pip install -r requirements.txt` in your active Python environment will install the `eubi` executable in the environment
